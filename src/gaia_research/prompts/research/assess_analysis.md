Assess only the selected focus against the selected evidence packet. Produce new_claims for new answer candidates or synthesis claims that should become ordinary Gaia claim(...) objects, then produce a dense but grounded set of candidate relations rather than a minimal summary. Prefer to record all materially useful relations visible in the packet, including existing-existing relations between pulled Gaia claim refs and existing-new relations between pulled Gaia claim refs and new_claims[*].id. Do not invent endpoints or pad weak rows just to increase count. Treat each relation as a cross-disciplinary evidence matrix row: capture the claim, stance/type, epistemic_status, and when visible the system, condition, method, observable, certainty, and scope_note. Use relation.type for stance (`supports`, `opposes`, `qualifies`, `undercuts`, `background_for`, `needs_more_evidence`); do not repeat that stance in promotion_hint. promotion_hint is only a Gaia-promotion hint: use one of `derive`, `infer`, `depends_on`, `contradict`, `question`, `obligation`, or `none`; when unsure, use `none`. Endpoint/provenance rule: claim_refs are relation endpoints, while source_refs are provenance, citations, or audit trail for the judgment. Every candidate relation with concrete endpoints must include at least two claim_refs. Do not rely on source_refs to supply missing relation endpoints; downstream sync may repair malformed output, but your primary output must be complete. Only put a package_ref in claim_refs when that package_ref is a claim endpoint, not merely because it cites, motivates, or supports the relation; keep other supporting package_refs in source_refs. When a relation has concrete endpoints, set claim_refs using new_claims[*].id for new local claims and Gaia pulled-claim refs such as lkm:<package>::<label> from resolved anchors or package_ref.ref; never use dotted refs such as import_name.symbol. Prioritize evidence items and paper leads marked `"is_new": true`; explicitly decide whether each materially relevant new item supports, qualifies, undercuts, or should be omitted as search noise. Do not write the final review prose; later report phases will write the article from this judgment. Emit limitations, next queries, and at most 2 candidate obligations that directly follow from missing or conflicting evidence. Each obligation must state target `{kind: question|claim|relation, id/ref}`, action_type, action, content, and source_refs. Set obligation.actionable=true only for a near-term blocking task that should become an open inquiry obligation; otherwise it remains a deferred research gap.
